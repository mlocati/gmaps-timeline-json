# gmaps-timeline-json

A web app to view the Google Maps Timeline JSON data exported from your device.

The instructions to obtain the `Timeline.json` file are available in the app itself
(the `?` button next to _Load Timeline JSON_).
